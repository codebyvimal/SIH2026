# Dependencies / build artifacts
node_modules/
.vite/
.next/
out/
dist/
build/
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/
.ruff_cache/

# Environment & secrets (local only — never commit)
.env
.env.*
*.env
*.env.*
!.env.example

# OS / editor noise
.DS_Store
Thumbs.db
.idea/
.vscode/

# Logs
*.log

# NOTE: .agents/, .claude/, and skills-lock.json are intentionally NOT ignored.
# They ship in the repo so every clone already has the skills and rules.
data/dummy/*.db
data/dummy/*.gpickle

# Scratch runner scripts
run_*.sh
